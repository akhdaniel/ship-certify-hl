# This is an example environment file.
# For production, copy this to a file named .env and replace the placeholder
# with a strong, randomly generated secret.
# You can generate a suitable secret with: openssl rand -base64 32

JWT_SECRET=your_super_secret_and_long_random_string_here
